# Palo Alto PAN-OS — SAMPLE CONFIG WITH KNOWN VIOLATIONS

set deviceconfig system hostname PA-FW-01
set deviceconfig system domain example.com
set deviceconfig system dns-setting servers primary 8.8.8.8

set deviceconfig system ntp-servers primary-ntp-server ntp-server-address 10.0.0.50

set network interface ethernet ethernet1/1 layer3 ip 203.0.113.10/30
set network interface ethernet ethernet1/2 layer3 ip 10.1.1.1/24

set rulebase security rules allow-all from any to any source any destination any application any service any action allow
set rulebase security rules allow-all log-end no

set deviceconfig setting management http

set network profiles zone-protection-profile default
set rulebase security rules deny-all from any to any source any destination any application any service any action deny
